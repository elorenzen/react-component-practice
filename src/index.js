import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return (
        <div>Basic component setup</div>
    )
};

ReactDOM.render(<App />, document.querySelector('#root'));